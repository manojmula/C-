/**
 * There are 5 types of recursions
 * Tail recursion
 * Head recursion
 * tree recursion
 * Indirect recursion
 * Nested recursion
 * 
 * TAIL RECURSION- if a recursive function calling itself and it is the last statment in the function
 * then it is called tail recursionall the operation is performed on calling time so it is called tail recrsion
 * operation on the same line are ddone on returning timethe next statment will all be performed after the recursion function is executed
 * 
 * loop can be converted into tail recursion
 * 
 * **/