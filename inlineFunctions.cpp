/***
 * Inline functions
 * Function calls hav a certain amount of overhead
 * you sae what happens
 * on the call stack
 * sometimes we have simple functions
 * we can suggest to the compiler to compile them inline
 * avoid function call overhead
 * generate inline assemble code
 * faster 
 * could cuase code bloat
 * compilers optimization are very sophisticated
 * will likely even inline without your suggestion
 * 
*/