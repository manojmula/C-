/**
 * this pointer
 * this is a reserved keyword
 * contains the address of the object - so its a pointer to the object
 * can only be used in calss scope
 * all member access is done via the this pointer
 * 
 * can be used by the programmer
 * to access data member and methods
 * to determine if two objects are the same
 * can be dereferenced(*this ) to yeald the current object
 * **/
#include<iostream>

using namespace std;

int main()
{
    cout<<"hello";
}